#!/usr/bin/env sh
set -eu

if pgrep -u "$(id -u)" -f "[d]otfiles-power-menu" >/dev/null 2>&1; then
  pkill -u "$(id -u)" -f "[d]otfiles-power-menu"
  exit 0
fi

if command -v walker >/dev/null 2>&1; then
  exec dotfiles-power-menu
fi

exec dotfiles-popup Power dotfiles-power-menu
